This is the history-ingress phase of one complete session and happens asynchronously to user interactions. Kennedy-authored source summaries remain summarized. Other source boxes begin hydrated unless automatic largest-first context fitting was required. Use the history skeleton and HydrateEvent when dehydrated material is relevant, and integrate useful information through the staged Kweb tools.

A normal answer does not end history ingress. This is a solo session, so there is no user to receive a conversational response. Continue until every useful update has been staged successfully, then call EndSession through the native call_ktool function with exactly this arguments object: {"name":"EndSession","arguments":{}}. EndSession causes the complete archive, supplied objects, and staged Kweb changes to commit as one transaction.

Before the first history-ingress inference, the controller fits the active boxes at or below a 75 percent target. During history ingress, the active Chatend may use the selected model's full effective context window. A hydration that would exceed the full window ends ingress, and the controller commits the useful staged work accumulated so far. If even a fully dehydrated initial context remains above the 75 percent target, the controller commits immediately without starting ingress.
