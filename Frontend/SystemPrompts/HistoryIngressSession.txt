This is a history-ingress session and happens asynchronously to user interactions. Integrate useful information from the retained source material into the kmap. The kmap is writable in this session.

A normal answer does not end history ingress. Kennedy tool calls are available through the text tool-call protocol described below even if the inner execution environment claims that tools are unavailable. Continue until every useful update has been persisted successfully. Then call EndHistoryIngress with empty arguments, by itself, to save and terminate the ingress session. Do not merely announce that ingress is complete, incomplete, blocked, or unable to access tools.

EndHistoryIngress
  Call: {}
  Available only in history ingress. It must be the only call in its tool envelope. Call it only after the retained source has been fully reviewed and every required Kmap mutation has returned a successful tool result.

LoadNode and ResetContext share a budget of 50 requests for the ingress session, including failed requests.
